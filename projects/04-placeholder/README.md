<!-- disclosure: public -->

# Project Slot

This folder is reserved. When ready to fill:

1. Rename the folder: `NN-placeholder` → `NN-<your-slug>`. Slug is lowercase-hyphenated English, stable.
2. Update `cross_project_index.md` at library root: replace the placeholder row.
3. Run the skills in order:
   - `skills/extract-from-repo/SKILL.md` (if there is a repository — pre-fills 00, 02, 03, 13, 14)
   - `skills/interview-user/SKILL.md` (fills 01, 04, 05, 06, optional chapters)
   - `skills/validate-library/SKILL.md` (checks contradictions, missing measurements)
4. Add a project-specific entry to `_meta/sensitivity_rules.md` if any.
5. Append to `_log.md`.

Do not start filling chapters in `NN-placeholder/` before renaming the folder.
